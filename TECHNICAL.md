# Flow Matching from Scratch — 技术文档

CIFAR-10 上从零实现 stochastic flow matching 的复盘记录。本文档不展开 flow matching 的数学背景，专注于工程实现里实际遇到的问题与决策。

---

## 1. 项目框架

### 目录结构

```
flow_matching_from_scratch/
├── pyproject.toml                # 包元信息, 用 pip install -e . 让 src 可被导入
├── README.md                     # (留空, 待补)
├── .gitignore
├── assets/                       # 采样图 / 可视化输出
├── checkpoints/                  # 训练保存的 .pt 文件
├── src/
│   ├── flow/                     # flow matching 算法本体 (与数据/网络解耦)
│   │   ├── interpolant.py        # 插值系数 α(t)/β(t) 与噪声放大 σ(t)
│   │   └── flow_matching.py      # loss 计算 + 反向 SDE 采样器
│   ├── model/                    # 速度场 / 分数场共用的 U-Net
│   │   ├── modules.py            # ResBlock / self-attention / 时间嵌入 / 上下采样
│   │   └── U_net.py              # U-Net 主体 (照搬 diffusion 项目)
│   └── training/
│       └── trainer.py            # 训练循环 + EMA + checkpoint, 走 accelerate 多卡
└── scripts/
    └── sample_ckpt.py            # 加载 ckpt, 从噪声反向 SDE 采样, 存成 8x8 网格
```

### 数据流

**训练**:
```
x_1 (CIFAR-10 真实图)  ─┐
                       ├─→ interp(x_0, x_1, t) ─→ (x_t, xdot_t, σ, z)
x_0 ~ N(0, I)         ─┘                           │
                                                   ├─→ net_v(x_t, t) → v_pred → L_v = ‖v_pred − xdot_t‖²
                                                   └─→ net_s(x_t, t) → s_pred → L_s = ‖σ·s_pred + z‖²
```

**采样** (反向 SDE):
```
x_0 ~ N(0, I)
for t in 0, dt, 2dt, ..., 1:
    drift     = (v(x_t, t) + 0.5·σ(t)²·s(x_t, t)) · dt
    diffusion = σ(t) · √dt · ε,  ε ~ N(0, I)        # 最后一步置零
    x_{t+dt} = x_t + drift + diffusion
得到 x_1
```

### 关键配置

| 部件 | 配置 |
|---|---|
| U-Net | base_channels=128, embedded_dim=512, 4 级 encoder/decoder, 中间两级带 self-attention |
| 插值方案 | linear: α(t)=t, β(t)=1−t |
| 噪声放大 | σ(t) = √(2t(1−t)) |
| 时间采样 | Beta(2, 2), 截断到 [0.01, 0.99] |
| 优化器 | AdamW, lr=2e-4, 双网络各一份 |
| EMA | decay=0.9999, 双网络各一份 shadow |
| 训练 | 300 epoch, batch=384, accelerate 多卡 |
| 采样 | 反向 SDE, steps=200 |
| 数据 | CIFAR-10, T.Normalize((0.5,)*3, (0.5,)*3) → [-1, 1] |

### 入口命令

```bash
pip install -e .                                    # 一次性, 让 src 可被导入
accelerate launch --num_processes=2 -m src.training.trainer    # 训练
python scripts/sample_ckpt.py --n 64 --steps 200    # 采样
```

---

## 2. 每份代码做什么 / 怎么实现

### 2.1 [src/flow/interpolant.py](src/flow/interpolant.py)

负责定义"从 x_0 到 x_1 的带噪声插值轨迹"以及它对 t 的导数。整个文件不依赖任何网络结构，是纯粹的数学工具。

#### `noise_magnify(t)` — 噪声放大函数 σ(t)

设计动机:flow matching 的反向过程不仅要有**漂移项**(由速度场 v 提供方向),也要有**扩散项**(注入随机性,避免模式坍缩)。这要求前向插值时在每一步都叠加一个正态噪声 z,得到带噪声的插值轨迹:

```
x_t = α(t)·x_1 + β(t)·x_0 + σ(t)·z,    z ~ N(0, I)
```

噪声幅度 σ(t) 不能在端点处也很大 —— 在 t=0 时 x_t 应该等于 x_0(纯噪声本身),在 t=1 时 x_t 应该等于 x_1(干净图像),两端再叠噪声会破坏边界条件。所以 σ(t) 需要是一条**两端为 0、中间最大**的钟形曲线。本项目沿用 Albergo et al. 的选择:

```
σ(t) = √(2t(1-t))     # t=0/t=1 时为 0, t=0.5 时取最大值 1
```

函数同时返回 σ 对 t 的导数 σ̇,因为整条插值轨迹对 t 求导后会用到:

```
xdot_t = α̇(t)·x_1 + β̇(t)·x_0 + σ̇(t)·z
```

这里 `(α̇, β̇, σ̇)` 各自是对应系数对 t 的导数,分别乘以 `(x_1, x_0, z)`,这就是训练时速度场要拟合的"真值"。

**端点数值问题**:σ̇(t) = (1−2t)/√(2t(1−t)) 在 t=0、t=1 时分母为 0,会得到 NaN。`endpoint_mask` 在这两点把 σ 和 σ̇ 强制设为 0,绕开除零。训练时 t 从 [0.01, 0.99] 采样本身就避免了端点,但 mask 是兜底。

#### `linear_coeffs(t)` 和 `trig_coeffs(t)` — 两种插值方案

把上面的公式参数化:

| 方案 | α(t) | β(t) | α̇(t) | β̇(t) |
|---|---|---|---|---|
| `linear_coeffs` | t | 1−t | 1 | −1 |
| `trig_coeffs` | sin(πt/2) | cos(πt/2) | (π/2)cos(πt/2) | −(π/2)sin(πt/2) |

线性方案最简单,边界条件 α(0)=0、α(1)=1 自动满足;三角方案在端点的导数为 0,过渡更"光滑"。本项目默认用 linear。

#### `interp(x_0, x_1, t, ...)` — 一次给出 x_t 和 xdot_t

把上面所有零件拼起来:采一个 z,算出 x_t 和 xdot_t,顺便返回当前 σ 和 z(后面 score loss 要用)。这是训练循环里调用的统一入口。

### 2.2 [src/flow/flow_matching.py](src/flow/flow_matching.py)

flow matching 的算法本体: 损失计算 + 反向采样器。不依赖具体网络结构,只要传进来的 `net_v` / `net_s` 满足 `(x, t) → 同形状输出` 即可。

#### `sample_t` — 时间采样:为什么用 Beta(2, 2) 而不是 U(0, 1)

```python
t = Beta(2.0, 2.0).sample((B, 1))
t = eps_t + (1 - 2*eps_t) * t       # 线性映射到 [0.01, 0.99]
```

**Beta(2, 2)** 的密度是 `f(t) = 6·t·(1-t)`,在 [0, 1] 上是一条**倒扣的钟形曲线**:中间 (t≈0.5) 概率最大,端点为 0。这其实是**重要性采样**:把训练算力集中到信息量更大的 t 区间。

为什么端点信息量小:
- **t→0** 时 `x_t ≈ x_0` (纯噪声),网络看到的输入退化,无法学到有意义的速度场
- **t→1** 时 `x_t ≈ x_1` (干净图像),同理
- **score loss 在端点没梯度**: `loss_s = (σ·s + z)²`,而 σ(0)=σ(1)=0,整个 loss 跟 s 的预测无关,这块训练等于浪费

中间区域 (t≈0.5) 才是网络真正需要"学"的地方:噪声和数据混合,网络必须理解图像结构才能正确预测。

`eps_t=0.01` 是再一道保险,把 t 硬截断到 [0.01, 0.99],绕开端点附近 σ̇ = (1−2t)/√(2t(1−t)) 分母→0 的数值问题。

#### `compute_loss` — 速度损失 L_v 与分数损失 L_s

```python
loss_v = ((v_pred - xdot_t) ** 2).mean()
loss_s = ((s_pred + z) ** 2).mean()
```

**速度损失 L_v**:

`xdot_t = α̇·x_1 + β̇·x_0 + σ̇·z` 是**条件速度场** —— 给定具体的 (x_0, x_1, z) 时,插值轨迹在 (x_t, t) 处的瞬时速度。

但训练时的 L2 期望意义下,网络学到的是**边际速度场**:

```
v*(x_t, t) = E[xdot_t | x_t, t]
```

也就是连续性方程 ∂_t p_t + ∇·(p_t · v) = 0 中的速度场,它把初始正态分布 p_0 输运到目标数据分布 p_1。这正是采样时需要的量 —— 反向 ODE `dx/dt = v(x, t)` 沿着这个速度场积分,从 x_0 ~ N(0, I) 到达 x_1。

为什么训练 target 依赖 (x_0, x_1, z),采样时只有 x_t 也能用:**L2 损失 + 期望**让网络自动边缘化掉 (x_0, x_1, z),学出只依赖 (x_t, t) 的边际量。这是 score matching 风格方法的统一逻辑。

**分数损失 L_s**:

理想目标是真实分数 ∇log p_t(x_t),但 p_t 本身就是未知的(它是给定时刻 t 的边际分布,需要对所有可能的 (x_0, x_1) 积分边缘化,无解析形式)。所以无法直接监督。

替代方案:用**条件分数**做 target。给定 (x_0, x_1) 时,x_t 服从 N(α·x_1 + β·x_0, σ²·I),求导得:

```
∇_{x_t} log p(x_t | x_0, x_1) = -z / σ
```

denoising score matching 定理:用条件分数 `-z/σ` 做 L2 训练,期望意义下学到的就是真实边际分数 ∇log p_t —— 跟 v 同构。

**参数化:为什么预测 -z 而不是 -z/σ**:

直接让网络输出真实分数 `s = -z/σ` 在数学上正确,但 σ 在端点很小(σ(0.01) ≈ 0.14),target `-z/σ` 量级 ≈ 7,**方差随 t 变化巨大**,训练不稳定,容易梯度爆炸把网络学崩。

改造:让网络学 `s_pred = σ·s`,等价于直接预测 `-z`,target 始终是标准正态、**方差有界**。采样时再除回 σ 还原真分数。SDE 反向公式 `drift = (v + 0.5·σ²·s)·dt` 相应改成 `drift = (v + 0.5·σ·s_pred)·dt`(因为 σ²·s = σ·s_pred)。

这就是 DDPM 的 noise-prediction 参数化思路:**让网络学一个方差稳定的代理量,而不是直接学物理上正确但数值上恶劣的真量**。本项目从 `(σ·s + z)²` 改成 `(s_pred + z)²` 是为了避免 score 网络在训练中后期塌缩到输出 ≈ 0 的退化解。

#### `sde_sample` — 反向 SDE 采样

```python
drift = (v + 0.5 * sigma_t * s) * dt
diffusion = sigma_t * (dt ** 0.5) * torch.randn_like(x)
if i == steps - 1:
    diffusion = 0.0
x = x + drift + diffusion
```

整条 SDE:

```
dx = (v + 0.5·σ²·s_true) dt + σ·dW
   = (v + 0.5·σ·s_pred) dt + σ·dW       (s_pred = σ·s_true 参数化下)
```

**drift 里的 0.5·σ²·s — Itô 修正项**:

纯 ODE `dx = v dt` 把噪声分布 p_0 输运到数据分布 p_1。但加了随机扰动 σ·dW 后,这个新过程**在每个 t 上的边际分布不再是 p_t**(扩散把密度打散了)。要让带噪过程仍保持每个时刻的边际分布 p_t 不变,必须按 Fokker-Planck 方程补一个反向修正项,大小恰好是 `0.5·σ²·∇log p_t = 0.5·σ²·s`。直观上:**score 把分布"拉回密度高的地方",抵消扩散注入的发散趋势**。

三个角色:
- `v` — 确定性骨架,提供从 x_0 到 x_1 的方向
- `σ·dW` — 随机探索,提高样本多样性、避免模式坍缩
- `0.5·σ²·s` — Itô 修正,保持分布不变(让扰动+修正后的过程仍在数据流形附近)

**diffusion 用 √dt 而不是 dt — 布朗运动标度律**:

布朗运动 W(t) 的核心性质是 `Var(W(t+dt) - W(t)) = dt`,微分形式 `dW ~ √dt·N(0, I)`。离散化时若错写成 `σ·dt·z`,跑 N = 1/dt 步累计噪声方差只有 N·dt² = dt → 0,**dt → 0 时随机性彻底消失,SDE 退化成 ODE**。正确的 √dt 让累计方差保持有限的常数。

**最后一步置零 diffusion**:

三个理由:
1. **训练分布外**:训练 t 截断到 [0.01, 0.99],t→1 时网络从未训过,注入噪声没有意义
2. **避免污染**:最后一步若仍加噪,输出永远带最后一步的随机扰动,样本模糊
3. **数值稳定**:即使 σ(t→1)→0,有限 dt 下仍有微小残留,直接置零让落地干净

等价于"用确定性方式收尾",这是 DDPM / flow matching 的标准做法。



## 3. 技术分析

记录从 MNIST 单文件版搬到 CIFAR-10 完整项目的过程中,踩到的几个工程性 bug 和它们暴露的方法论问题。

### 3.1 复用代码 ≠ 代码正确

MNIST 单文件版的 `interp` 直接被复制到了 CIFAR 项目的 `interpolant.py`,以为公式都对。实际上有两个 bug 在 MNIST 设置下被掩盖、在 CIFAR 设置下暴露。

**Bug 1: `xdot_t` 含 σ̇·z 干扰项**

公式上 `xdot_t = α̇·x_1 + β̇·x_0 + σ̇·z`,数学上这是 dx_t/dt 的完整表达式,σ̇·z 的期望为 0 不影响最优解。但工程上:

- σ̇(t) 在端点处发散(t=0.01 时 σ̇ ≈ 7),target 量级大
- 单 batch 内 z 的随机性让 target 方差暴涨
- v 网络在"期望意义对的 target"加上"实际很噪"的样本上挣扎,单步 loss 波动 ±0.3

**MNIST 下为什么没暴露**: batch=32 + 数据简单,σ̇·z 的方差被批平均稀释。CIFAR batch=320 反而更容易暴露端点 t 偶尔被采到的方差冲击。

**修复**: 训练 target 改成 `xdot_t = α̇·x_1 + β̇·x_0`,把 σ̇·z 拿掉。这是 Albergo 论文 / Lipman 2022 / 各家官方实现的标准写法。

**Bug 2: sinusoidal_embedding 在 t ∈ [0,1] 区间高频项失效**

`sinusoidal_embedding` 来自 DDPM 项目,DDPM 里 t 是整数 ∈ [0, 999]。flow matching 的 t ∈ [0, 1],直接传进去:

- 频率 freq[i] = exp(-2·log(10000)·i/D),高频(i 大时)freq 极小
- args = t·freq → t=0.5 时 args[128] ≈ 5e-5
- sin(5e-5) ≈ 5e-5,**所有高频维度几乎是 0**
- 网络拿到的时间嵌入只有低频几位有信号

**后果**: 网络无法精细区分 t=0.1 和 t=0.5,变成"t-blind"的回归——只能学到 t 上的平均速度场。loss 卡在某个值不动正是这个表现。

**修复**: 进 sinusoidal_embedding 之前 `t_scaled = t * 1000.0`,让全频谱都有信号。

**两个 bug 的合计代价**: 70 epoch 的训练时间 + 反复改 DDP / lr / EMA 的盲修代价,所有这些在 bug 修复前**都是无效操作**。

### 3.2 score loss 参数化:预测 z 还是预测 -z/σ

预测真实分数 `s_true = -z/σ` 在数学上是直接的,但 σ→0 时 target 量级 → ∞,score 网络容易学崩(我们实际看到 loss_s 突然锁死在 1.0,网络输出退化到 0)。

切换到 noise-prediction 参数化(网络学 `s_pred = -z`,等价于 σ·s_true):

- target 始终是 N(0, I),量级稳定
- 采样公式相应调整: drift 里原本的 `0.5·σ²·s_true` 改成 `0.5·σ·s_pred`
- 这是 DDPM 一直采用的 ε-prediction 思路,不是新东西,但搬到 stochastic flow matching 时容易忽略

### 3.3 训练 target 方差分析应该是设计阶段动作

总结 3.1 + 3.2,两个 bug 的共性是:某个公式数学上正确,但 target 方差在某些 t 下过大/不稳。**做训练 target 设计时,该把 Var(target) 作为 t 的函数画一遍**,而不是写完跑起来再看 loss 曲线发现问题。

如果当时画过 σ̇(t) 在 t 接近端点的 max 值,Bug 1 在写第一行代码时就该被发现。

### 3.4 ODE vs SDE 取舍

实测对比(在修 bug 之前):

- **SDE (v + s)**: 模型不完美时反而更稳。score 注入的修正项能补偿 v 的小偏差。但需要训两个网络,计算量翻倍。
- **ODE (only v)**: 路径完全确定。如果 v 学得好,ODE 出图比 SDE 更"硬";如果 v 学得不好,ODE 就一路偏到天上去。

**判断准则**: 训练量充分(至少 100+ epoch)+ ODE 和 SDE 出图差不多 → 用 ODE,省一半模型;训练量不足或 v 网络容量受限 → 用 SDE,score 修正能把质量提上去。

本项目继续用 SDE 作为主采样路径,因为联合训练 v + s 已经做了。

### 3.5 监控指标决定 debug 速度

整个排查过程时间分布:

- **没监控阶段** (前期 60 epoch): 只看 epoch print 的 loss 数字,猜原因 + 改代码 + 重训等结果。**改一个等 30 分钟**。
- **加 grad norm 打印后**: 立刻看到 grad 远不到 max_norm,否定 "lr 太大"假设。**5 秒判断一个假设**。
- **加 wandb 后**: 看到 loss 单步波动幅度 vs 趋势的关系,定位 target 方差问题。**从打印数字升级到画时间序列分布**。

**结论**: 监控基础设施是 debug 的乘数。下个项目从 day 1 就接 wandb / TensorBoard,不是 plug-in,是必备。

---

## 4. AI 协作复盘

记录这次和 Claude 协作中真正起作用的几条原则,以及踩到的坑。

### 4.1 用户的实地观察比助手的纸面分析权重高

几个关键节点:

- **"epoch 60 反弹是正常吗"**: 我第一反应是把它合理化为"训练动态正常",用户坚持说不对。后来发现 target 含 σ̇·z 才是震荡的真正源头。
- **"改完 unwrap 似乎没效果"**: 我用 DDP 修复理论解释为什么应该有效,但用户实测说没变化,事实证明单修 DDP 不解决问题。
- **"ODE 也没学到东西,难道是参数量"**: 我先否定参数量假设,但用户的"网络好像分不清不同 t"直觉,正好对应了 sinusoidal_embedding 失效的根因。

**经验**: 用户跑出的现象 > 助手的推理。当用户说"这不对",不要急着用理论合理化。

### 4.2 修一个改一个,每步要能画句号

错误模式: 一次性改 5 个东西,跑起来看 loss 还是不降,根本不知道哪个起作用 / 哪个是负向的。

正确模式: 每次只改一个变量,**用具体指标验证**:

- 改 DDP unwrap → 看 GPU util 是否两卡都满 (✅)
- 改 score loss target → 看 loss_s 是否还会突然锁死 (✅ 不会了)
- 改 xdot_t 不含 σ̇·z → 看单步 loss 震荡幅度 (✅ 从 ±0.3 降到 ±0.05)
- 改 t 缩放 *1000 → 看 loss 是否能突破之前的平台 (✅)

**每一步都要能闭环**。不闭环的修改是技术债。

### 4.3 AI 给方案要追问"代价"

第一次让 AI 修 score loss 时,直接给了"预测 -z 而不是 -z/σ"。后来才意识到必须 spell out:

- 这个改动需要重训(原 ckpt 的 score 网络在另一种参数化下,无法续训)
- 采样公式必须同步调整,否则采样会用错的 σ 系数
- 是否有等价但更简单的方案(比如只改 lr_s)

**经验**: AI 提"最佳方案"时容易省略代价。决策点上必须主动追问"如果做这个,我要付出什么"。

### 4.4 防御性代码要警惕

排查过程中 AI 多次建议加各种"兜底":

- 加梯度裁剪(✅ 必要,确实在防爆炸)
- 加 NaN 检测(✗ 当时没真出 NaN,加了徒增复杂度)
- 改 fp16 / bf16(✗ 用户记得 diffusion 时遇过 fp16 underflow,谨慎拒绝)
- 加 scheduler / warmup(✗ 当前问题是 target 错,不是 lr 调度)

**经验**: 只修被验证有问题的,不修可能有问题的。每加一层防御代码都增加了未来 debug 的复杂度。

### 4.5 监控基础设施有溢价

加 wandb 时花了 30 分钟(注册账号 + login + 集成 + 启动),当时觉得"是不是过度工程"。实际上:

- 不到 1 小时就靠 wandb 曲线发现"loss 平稳但单步波动 25%"这个之前看不到的症状
- 这个症状直接指向 target 噪声大,接下来的修复都基于这个观察

**经验**: 监控不是项目可选项,是基础设施。下次新项目从第一天就上 wandb,把它当作 git 一样不可省略的工具。

### 4.6 AI 的诊断要自己核实

排查过程中 AI 给过几个错误推测:

- "loss_s 锁定到 1.0 是 NaN 导致" → 跑 NaN 检测,实际上权重正常
- "loss_v=1.2 已经学到 60% 方差,健康" → 但单步震荡 ±0.3 才是真问题,不是绝对数值
- "lr 太大" → 看 grad/v 远不到 max_norm,否定

**经验**: AI 给假设要给"快速验证命令"(30 秒能跑完),先验证再行动。不能拿到一句假设就改代码。

---


